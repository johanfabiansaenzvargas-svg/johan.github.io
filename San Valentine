<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Para ti culona hermosa, feliz San Valentín ❤️</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            overflow-x: hidden;
        }

        /* Corazones flotantes de fondo */
        .background-hearts {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
        }

        .floating-heart {
            position: absolute;
            font-size: 30px;
            opacity: 0.3;
            animation: float 15s infinite;
        }

        @keyframes float {
            0%, 100% {
                transform: translateY(100vh) rotate(0deg);
                opacity: 0;
            }
            10% {
                opacity: 0.3;
            }
            90% {
                opacity: 0.3;
            }
            100% {
                transform: translateY(-100px) rotate(360deg);
                opacity: 0;
            }
        }

        .container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 30px;
            padding: 40px 30px;
            max-width: 500px;
            width: 100%;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            position: relative;
            z-index: 1;
        }

        .message-section {
            text-align: left;
            margin-bottom: 30px;
            color: #333;
        }

        .message-section h2 {
            font-size: 1.3rem;
            color: #764ba2;
            margin-bottom: 20px;
            font-weight: 600;
        }

        .message-section p {
            font-size: 1.1rem;
            line-height: 1.8;
            margin-bottom: 15px;
            color: #555;
        }

        .signature {
            font-size: 1rem;
            color: #888;
            font-style: italic;
            margin-top: 20px;
        }

        /* Árbol de corazones */
        .tree-container {
            position: relative;
            width: 100%;
            height: 300px;
            margin: 30px 0;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Iniciales grandes F+M */
        .initials-large {
            font-size: 10rem;
            font-weight: bold;
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a6f 50%, #ff6b6b 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-family: 'Georgia', serif;
            text-shadow: 0 10px 30px rgba(255, 107, 107, 0.3);
            letter-spacing: 0.1em;
            animation: glow 2s infinite;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100%;
        }

        @keyframes glow {
            0%, 100% {
                filter: drop-shadow(0 0 20px rgba(255, 107, 107, 0.6));
            }
            50% {
                filter: drop-shadow(0 0 40px rgba(255, 107, 107, 0.9));
            }
        }

        /* Mini corazones en el árbol */
        .mini-heart {
            position: absolute;
            width: 20px;
            height: 20px;
            animation: pulse 2s infinite;
        }

        .mini-heart:before,
        .mini-heart:after {
            content: "";
            position: absolute;
            top: 0;
            width: 10px;
            height: 16px;
            border-radius: 10px 10px 0 0;
        }

        .mini-heart:before {
            left: 5px;
            transform: rotate(-45deg);
            transform-origin: 0 100%;
        }

        .mini-heart:after {
            left: 0;
            transform: rotate(45deg);
            transform-origin: 100% 100%;
        }

        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.2);
            }
        }

        /* Contador */
        .counter-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 25px;
            border-radius: 20px;
            text-align: center;
            margin-top: 30px;
        }

        .counter-section p {
            font-size: 1rem;
            margin-bottom: 15px;
            opacity: 0.9;
        }

        .counter {
            font-size: 1.8rem;
            font-weight: bold;
            letter-spacing: 2px;
        }

        .time-unit {
            display: inline-block;
            margin: 0 5px;
        }

        .time-label {
            font-size: 0.7rem;
            display: block;
            margin-top: 5px;
            opacity: 0.8;
        }

        @media (max-width: 480px) {
            .container {
                padding: 30px 20px;
            }

            .message-section h2 {
                font-size: 1.1rem;
            }

            .message-section p {
                font-size: 1rem;
            }

            .tree-container {
                height: 250px;
            }

            .initials-large {
                font-size: 6rem;
            }
        }
    </style>
</head>
<body>
    <!-- Corazones flotantes de fondo -->
    <div class="background-hearts" id="bgHearts"></div>

    <div class="container">
        <div class="message-section">
            <h2>Para ti culona hermosa, feliz San Valentín:</h2>
            <p>Atolondrado y confuso, demasiado lleno de ruidos, sin centro ni reposo, desconectado del otro lado de la piel, aturdido por el interminable crujir de este corazón, tierra cuarteada, ceniza gris en el pecho, así pasan estas noches de calor y duermevela, estas noches en que no estoy contigo.</p>
            <p class="signature">— Con amor 💕</p>
        </div>

        <!-- Iniciales grandes F+M -->
        <div class="tree-container">
            <div class="initials-large">F+M</div>
            <!-- Mini corazones decorativos -->
            <div class="mini-heart" style="top: 20px; left: 15%; background: linear-gradient(135deg, #ff9a9e, #fad0c4);"></div>
            <div class="mini-heart" style="top: 40px; left: 80%; background: linear-gradient(135deg, #fbc2eb, #a6c1ee); animation-delay: 0.5s;"></div>
            <div class="mini-heart" style="top: 200px; left: 20%; background: linear-gradient(135deg, #ff6b6b, #ee5a6f); animation-delay: 1s;"></div>
            <div class="mini-heart" style="top: 220px; left: 75%; background: linear-gradient(135deg, #ffecd2, #fcb69f); animation-delay: 1.5s;"></div>
        </div>

    </div>

    <script>
        // Crear corazones flotantes de fondo
        function createFloatingHearts() {
            const container = document.getElementById('bgHearts');
            const hearts = ['💕', '💖', '💗', '💝', '❤️'];
            
            for (let i = 0; i < 15; i++) {
                const heart = document.createElement('div');
                heart.className = 'floating-heart';
                heart.textContent = hearts[Math.floor(Math.random() * hearts.length)];
                heart.style.left = Math.random() * 100 + '%';
                heart.style.animationDelay = Math.random() * 15 + 's';
                heart.style.fontSize = (Math.random() * 20 + 20) + 'px';
                container.appendChild(heart);
            }
        }

        // Inicializar
        createFloatingHearts();
    </script>
</body>
</html>
